<?xml version="1.0" encoding="utf-8"?>
<Project DefaultTargets="Run"
         xmlns="http://schemas.microsoft.com/developer/msbuild/2003"
         ToolsVersion="4.0">
  <ImportGroup>
    <Import Project="$(MSBuildProjectDirectory)\lib\trove\Framework\v2.0\MSBuild.Community.Tasks.Targets" />
    <Import Project="$(MSBuildProjectDirectory)\lib\trove\Framework\v3.5\MSBuild.ExtensionPack.tasks" />
  </ImportGroup>
  <ImportGroup>
    <Import Project="$(MSBuildProjectDirectory)\product.xml" />
    <Import Project="$(MSBuildProjectDirectory)\msbuild\Trove.xml" />
    <Import Project="$(MSBuildProjectDirectory)\msbuild\Zip.xml" />
    <Import Project="$(MSBuildProjectDirectory)\msbuild\trove\Versioning.xml" />
  </ImportGroup>
  <PropertyGroup>
    <Configuration Condition="'$(Configuration)'==''">Release</Configuration>
    <BuildName>$(Configuration) $(TargetFrameworkVersion)</BuildName>
  </PropertyGroup>
  <PropertyGroup>
    <ClassLibrariesPath>$(MSBuildProjectDirectory)\src\ClassLibraries</ClassLibrariesPath>
    <ConsoleApplicationsPath>$(MSBuildProjectDirectory)\src\ConsoleApplications</ConsoleApplicationsPath>
    <InstallationPath>$(MSBuildProjectDirectory)\src\Installation</InstallationPath>
  </PropertyGroup>
  <Target Name="Run" DependsOnTargets="Versioning">
    <CallTarget Targets="Trove" />
    <CallTarget Targets="Zip" />
  </Target>
</Project>